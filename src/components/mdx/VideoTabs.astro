---
import VideoTabsSeo from "./VideoTabsSeo.astro";

interface Props {
  youtubeHref?: string;
  vkHref?: string;
  rutubeHref?: string;
}

const { youtubeHref, vkHref, rutubeHref } = Astro.props as Props;
---

<VideoTabsSeo youtubeHref={youtubeHref} vkHref={vkHref} rutubeHref={rutubeHref} />
