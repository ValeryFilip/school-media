<section class="p3 benefits" aria-label="Бенефиты">
  <div class="wrap">
    <header class="head">
      <h2>Что получишь после одобрения</h2>
      <p>Полный набор, чтобы легко продвигать и стабильно получать выплаты.</p>
    </header>

    <div class="grid">
      <article class="card card--wide">
        <div class="pillRow">
          <span class="pill pill--accent">после одобрения</span>
          <span class="pill">старт</span>
        </div>
        <h3>Персональная ссылка / промокод</h3>
        <p>Удобная атрибуция и честный учёт оплат.</p>
      </article>

      <article class="card">
        <h3>Пак материалов</h3>
        <p>Тексты, баннеры, сценарии постов и сторис.</p>
      </article>

      <article class="card">
        <h3>Поддержка</h3>
        <p>Подскажем связки под твою аудиторию и формат.</p>
      </article>

      <article class="card">
        <h3>Статистика</h3>
        <p>Начислено / выплачено / период — в одном месте.</p>
      </article>

      <article class="card">
        <h3>Повышенные условия</h3>
        <p>Для сильных площадок — индивидуально.</p>
      </article>

      <article class="card">
        <h3>Регулярные выплаты</h3>
        <p>1–2 раза в месяц, по регламенту программы.</p>
      </article>
    </div>
  </div>
</section>

<style>
  /* typography via your globals */
  .p3.benefits {
    font-family: var(
      --ff,
      "Raleway",
      system-ui,
      -apple-system,
      Segoe UI,
      Roboto,
      Arial,
      sans-serif
    );
  }
  .head h2 {
    font-size: var(--h2, clamp(26px, 4vw, 42px));
    line-height: 1.2;
    margin: 0 0 var(--space-12, 12px);
    font-weight: 800;
    letter-spacing: -0.02em;
  }
  .head p {
    font-size: var(--text, clamp(14px, 1vw, 18px));
    line-height: 1.6;
    margin: 0;
    font-weight: 600;
    opacity: 0.86;
  }
  h3 {
    font-size: var(--h3, clamp(14px, 3.2vw, 20px));
    margin: 0 0 8px;
    font-weight: 900;
    letter-spacing: -0.01em;
  }
  .card p {
    font-size: var(--text, clamp(14px, 1vw, 18px));
    margin: 0;
    opacity: 0.82;
    line-height: 1.55;
    font-weight: 600;
  }

  .p3.benefits {
    margin: 20px;
    border-radius: 28px;
    background: #0b1020;
    color: #eaf0ff;
    padding: clamp(26px, 5vw, 56px) 0;
    position: relative;
    overflow: hidden;
  }
  .p3.benefits::before {
    content: "";
    position: absolute;
    inset: -2px;
    background:
      radial-gradient(
        520px 260px at 25% 20%,
        rgba(59, 130, 246, 0.16),
        transparent 60%
      ),
      radial-gradient(
        520px 260px at 80% 70%,
        rgba(34, 197, 94, 0.14),
        transparent 60%
      );
    pointer-events: none;
  }
  .wrap {
    position: relative;
    z-index: 2;
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 clamp(16px, 3vw, 28px);
  }
  .head {
    max-width: 72ch;
    margin-bottom: 18px;
  }

  /* ровный грид */
  .grid {
    display: grid;
    gap: 12px;
    grid-template-columns: repeat(12, 1fr);
    margin-top: 18px;
  }
  .card {
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    padding: 14px;
    backdrop-filter: blur(12px);
    grid-column: span 4;
  }
  .card--wide {
    grid-column: span 12;
  }

  .pillRow {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 10px;
  }
  .pill {
    padding: 7px 10px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    font-size: 12px;
    font-weight: 800;
    opacity: 0.92;
  }
  .pill--accent {
    background: linear-gradient(
      135deg,
      rgba(96, 165, 250, 0.3),
      rgba(167, 139, 250, 0.24)
    );
    border-color: rgba(255, 255, 255, 0.18);
  }

  @media (max-width: 980px) {
    .card {
      grid-column: span 6;
    }
    .card--wide {
      grid-column: span 12;
    }
  }
  @media (max-width: 560px) {
    .p3.benefits {
      margin: 12px;
      border-radius: 22px;
    }
    .grid {
      grid-template-columns: 1fr;
    }
    .card,
    .card--wide {
      grid-column: auto;
    }
  }
</style>
