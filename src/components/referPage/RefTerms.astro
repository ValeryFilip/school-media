<section class="p3 terms" aria-label="Terms">
  <div class="wrap">
    <div class="grid">
      <div class="card">
        <h2>Условия (коротко)</h2>
        <ul>
          <li>Вознаграждение: <b>до X%</b> (финально — после проверки)</li>
          <li>Выплаты: <b>1–2 раза/мес</b></li>
          <li>Учёт: <b>начислено / выплачено / период</b></li>
          <li>Запрещено: спам, обман, вводящие в заблуждение формулировки</li>
          <li>Поддержка: материалы + ответы по вопросам</li>
        </ul>
      </div>

      <div class="card accent">
        <h2>Почему есть модерация</h2>
        <ul>
          <li><b>Защита от спама</b> и “серых” методов</li>
          <li><b>Честная атрибуция</b> для выплат</li>
          <li><b>Сохранение доверия</b> к бренду</li>
          <li><b>Персональные советы</b> под аудиторию</li>
        </ul>
        <a class="btn" href="#apply">Подать заявку</a>
      </div>
    </div>
  </div>
</section>

<style>
  .p3.terms {
    margin: 20px;
    border-radius: 28px;
    background: #070a12;
    color: #eef2ff;
    padding: clamp(26px, 5vw, 56px) 0;
    position: relative;
    overflow: hidden;
  }
  .p3.terms::before {
    content: "";
    position: absolute;
    inset: -2px;
    background:
      radial-gradient(
        520px 260px at 20% 20%,
        rgba(249, 115, 22, 0.14),
        transparent 60%
      ),
      radial-gradient(
        520px 260px at 80% 60%,
        rgba(96, 165, 250, 0.16),
        transparent 60%
      );
    pointer-events: none;
  }
  .wrap {
    position: relative;
    z-index: 2;
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 clamp(16px, 3vw, 28px);
  }
  .grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    align-items: start;
  }
  .card {
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    padding: 14px;
    backdrop-filter: blur(12px);
  }
  .accent {
    background:
      radial-gradient(
        520px 260px at 70% 10%,
        rgba(96, 165, 250, 0.16),
        transparent 60%
      ),
      rgba(255, 255, 255, 0.06);
  }
  h2 {
    margin: 0 0 10px;
    font-size: clamp(22px, 3.2vw, 34px);
    letter-spacing: -0.02em;
    line-height: 1.1;
    font-weight: 950;
  }
  ul {
    margin: 0 0 12px;
    padding-left: 18px;
    display: grid;
    gap: 10px;
    color: rgba(238, 242, 255, 0.82);
    font-weight: 650;
    line-height: 1.5;
  }
  b {
    color: rgba(238, 242, 255, 0.95);
  }
  .btn {
    text-decoration: none;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    padding: 12px 16px;
    border-radius: 14px;
    font-weight: 950;
    color: #0b0f19;
    background: linear-gradient(135deg, #60a5fa, #a78bfa);
    box-shadow: 0 12px 40px rgba(96, 165, 250, 0.18);
  }
  @media (max-width: 980px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 560px) {
    .p3.terms {
      margin: 12px;
      border-radius: 22px;
    }
  }
</style>
