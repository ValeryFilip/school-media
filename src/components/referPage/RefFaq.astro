<section class="p3 faq" aria-label="FAQ">
  <div class="wrap">
    <div class="layout">
      <header class="left">
        <h2>FAQ</h2>
        <p class="lead">
          Ответы на вопросы, которые чаще всего мешают нажать “Подать заявку”.
        </p>
      </header>

      <div class="right">
        <details class="card" open>
          <summary>Почему нельзя получить ссылку сразу?</summary>
          <div class="a">
            Мы проверяем площадку и формат продвижения, чтобы защитить программу
            от спама и обеспечить честную атрибуцию.
          </div>
        </details>

        <details class="card">
          <summary>Сколько рассматривается заявка?</summary>
          <div class="a">Обычно 24–48 часов.</div>
        </details>

        <details class="card">
          <summary>Почему чекер не находит мой TG-ник?</summary>
          <div class="a">
            Вероятно, вы ещё не одобрены или указали другой ник. Подайте заявку
            — поможем связать данные.
          </div>
        </details>

        <details class="card">
          <summary>Я сменил TG-ник — что делать?</summary>
          <div class="a">
            Укажите ссылку на профиль в заявке или напишите в поддержку —
            обновим привязку.
          </div>
        </details>

        <details class="card">
          <summary>Когда выплаты?</summary>
          <div class="a">1–2 раза в месяц, по регламенту программы.</div>
        </details>

        <details class="card">
          <summary>Что запрещено?</summary>
          <div class="a">
            Спам, обман, ложные обещания и вводящие в заблуждение формулировки.
          </div>
        </details>
      </div>
    </div>
  </div>
</section>

<style>
  /* typography via your globals */
  .p3.faq {
    font-family: var(
      --ff,
      "Raleway",
      system-ui,
      -apple-system,
      Segoe UI,
      Roboto,
      Arial,
      sans-serif
    );
  }
  h2 {
    font-size: var(--h2, clamp(26px, 4vw, 42px));
    line-height: 1.2;
    margin: 0 0 var(--space-12, 12px);
    font-weight: 800;
    letter-spacing: -0.02em;
  }
  .lead {
    font-size: var(--lead, clamp(18px, 1vw, 20px));
    line-height: 1.6;
    margin: 0;
    font-weight: 600;
    opacity: 0.86;
  }
  summary {
    font-size: var(--h3, clamp(14px, 3.2vw, 20px));
  }
  .a {
    font-size: var(--text, clamp(14px, 1vw, 18px));
  }

  .p3.faq {
    margin: 20px;
    border-radius: 28px;
    background: #0b1020;
    color: #eaf0ff;
    padding: clamp(26px, 5vw, 56px) 0;
    position: relative;
    overflow: hidden;
  }
  .p3.faq::before {
    content: "";
    position: absolute;
    inset: -2px;
    background:
      radial-gradient(
        520px 260px at 20% 30%,
        rgba(167, 139, 250, 0.16),
        transparent 60%
      ),
      radial-gradient(
        520px 260px at 80% 70%,
        rgba(34, 197, 94, 0.12),
        transparent 60%
      );
    pointer-events: none;
  }
  .wrap {
    position: relative;
    z-index: 2;
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 clamp(16px, 3vw, 28px);
  }

  .layout {
    display: flex;
    gap: 12px;
    align-items: flex-start;
  }
  .left {
    flex: 0 0 36%;
    max-width: 36%;
  }
  .right {
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .card {
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    padding: 14px;
    backdrop-filter: blur(12px);
  }
  summary {
    cursor: pointer;
    font-weight: 900;
    list-style: none;
    letter-spacing: -0.01em;
  }
  summary::-webkit-details-marker {
    display: none;
  }
  .a {
    margin-top: 10px;
    opacity: 0.82;
    line-height: 1.6;
    font-weight: 600;
  }

  @media (max-width: 980px) {
    .layout {
      flex-direction: column;
    }
    .left {
      flex: 1 1 auto;
      max-width: none;
    }
  }
  @media (max-width: 560px) {
    .p3.faq {
      margin: 12px;
      border-radius: 22px;
    }
  }
</style>
