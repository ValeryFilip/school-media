<section class="p3stats" aria-label="Факты о программе">
  <div class="p3stats__wrap">
    <header class="p3stats__head">
      <h2 class="p3stats__h2">Факты о партнёрке</h2>
      <p class="p3stats__p">
        Коротко и по делу: как устроено и почему это честно.
      </p>
    </header>

    <div class="p3stats__grid">
      <article class="tile tile--hero">
        <div class="tile__kpi">до 15%</div>
        <div class="tile__t">вознаграждение за продажи</div>
        <div class="tile__d">
          Финальные условия — после проверки площадки. Никаких “серых” схем.
        </div>
        <div class="pillRow">
          <span class="pill">прозрачный учёт</span>
          <span class="pill">без спама</span>
          <span class="pill">поддержка</span>
        </div>
      </article>

      <article class="tile">
        <div class="tile__kpi">24–48ч</div>
        <div class="tile__t">модерация заявки</div>
        <div class="tile__d">После проверки выдаём ссылку/промокод.</div>
      </article>

      <article class="tile">
        <div class="tile__kpi">1–2×</div>
        <div class="tile__t">выплаты в месяц</div>
        <div class="tile__d">Регламент фиксируется в правилах.</div>
      </article>

      <article class="tile tile--wide">
        <div class="tile__t tile__t--big">Как считается доход</div>
        <div class="calcLine">
          <span class="tag">Бонус за учеников</span>
          <span class="x">+</span>
          <span class="tag">15% со Stepik</span>
          <span class="eq">=</span>
          <span class="tag tag--accent">Твой доход</span>
        </div>
        <div class="tile__d" style="margin-top:10px;">
          Для одобренных партнёров — чекер по TG-нику (агрегаты без персональных
          данных).
        </div>
      </article>

      <article class="tile tile--mini">
        <div class="tile__t" style="font-weight: 800;">Материалы</div>
        <div class="tile__d">Посты, баннеры, сценарии, связки.</div>
      </article>

      <article class="tile tile--mini">
        <div class="tile__t" style="font-weight: 800;">Статистика</div>
        <div class="tile__d">Начислено / выплачено / период.</div>
      </article>
    </div>
  </div>
</section>

<style>
  /* ===== Typography from your globals (with fallbacks) ===== */
  .p3stats {
    font-family: var(
      --ff,
      "Raleway",
      system-ui,
      -apple-system,
      Segoe UI,
      Roboto,
      Arial,
      sans-serif
    );
  }
  .p3stats__h2 {
    font-size: var(--h2, clamp(26px, 4vw, 42px));
    line-height: 1.2;
    margin: 0 0 var(--space-12, 12px);
    font-weight: 800;
    letter-spacing: -0.02em;
  }
  .p3stats__p {
    font-size: var(--text, clamp(14px, 1vw, 18px));
    line-height: 1.6;
    margin: 0;
    font-weight: 600;
    opacity: 0.86;
  }
  .tile__t {
    font-size: var(--h3, clamp(14px, 3.2vw, 20px));
  }
  .tile__d {
    font-size: var(--text, clamp(14px, 1vw, 18px));
  }

  /* ===== Layout / Style ===== */
  .p3stats {
    margin: 20px;
    border-radius: 28px;
    background: #0b1020;
    color: #eaf0ff;
    padding: clamp(26px, 5vw, 56px) 0;
    position: relative;
    overflow: hidden;
  }
  .p3stats::before {
    content: "";
    position: absolute;
    inset: -2px;
    background:
      radial-gradient(
        520px 260px at 20% 20%,
        rgba(59, 130, 246, 0.22),
        transparent 60%
      ),
      radial-gradient(
        520px 260px at 80% 60%,
        rgba(249, 115, 22, 0.2),
        transparent 60%
      );
    pointer-events: none;
    opacity: 0.85;
  }
  .p3stats__wrap {
    position: relative;
    z-index: 2;
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 clamp(16px, 3vw, 28px);
  }
  .p3stats__head {
    max-width: 72ch;
    margin-bottom: 18px;
  }

  .p3stats__grid {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 12px;
    margin-top: 18px;
  }
  .tile {
    grid-column: span 4;
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    padding: 14px;
    backdrop-filter: blur(12px);
    position: relative;
    overflow: hidden;
  }
  .tile::after {
    content: "";
    position: absolute;
    inset: auto -80px -90px auto;
    width: 240px;
    height: 240px;
    background: radial-gradient(
      circle at 30% 30%,
      rgba(167, 139, 250, 0.18),
      transparent 60%
    );
    transform: rotate(12deg);
    pointer-events: none;
  }
  .tile > * {
    position: relative;
    z-index: 1;
  }

  .tile--hero {
    grid-column: span 6;
  }
  .tile--wide {
    grid-column: span 12;
  }
  .tile--mini {
    grid-column: span 6;
  }

  .tile__kpi {
    font-size: clamp(22px, 2.8vw, 34px);
    font-weight: 900;
    letter-spacing: -0.02em;
    margin-bottom: 6px;
  }
  .tile__t {
    font-weight: 800;
    margin-bottom: 8px;
    opacity: 0.96;
  }
  .tile__t--big {
    font-size: var(--h3, clamp(14px, 3.2vw, 20px));
    font-weight: 900;
  }
  .tile__d {
    opacity: 0.82;
    font-weight: 600;
  }

  .pillRow {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 12px;
  }
  .pill {
    padding: 7px 10px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    font-size: 12px;
    font-weight: 800;
    opacity: 0.92;
  }

  .calcLine {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px;
    margin-top: 10px;
  }
  .tag {
    padding: 8px 10px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    font-weight: 800;
    font-size: 12px;
  }
  .tag--accent {
    background: linear-gradient(
      135deg,
      rgba(96, 165, 250, 0.3),
      rgba(167, 139, 250, 0.24)
    );
    border-color: rgba(255, 255, 255, 0.18);
  }
  .x,
  .eq {
    opacity: 0.85;
    font-weight: 900;
  }

  @media (max-width: 980px) {
    .tile--hero {
      grid-column: span 12;
    }
    .tile {
      grid-column: span 6;
    }
    .tile--mini {
      grid-column: span 6;
    }
    .tile--wide {
      grid-column: span 12;
    }
  }
  @media (max-width: 560px) {
    .p3stats {
      margin: 12px;
      border-radius: 22px;
    }
    .p3stats__grid {
      grid-template-columns: 1fr;
    }
    .tile,
    .tile--hero,
    .tile--wide,
    .tile--mini {
      grid-column: auto;
    }
  }
</style>
