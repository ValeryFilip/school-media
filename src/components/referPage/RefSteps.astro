<section class="p3 steps" aria-label="How it works">
  <div class="wrap">
    <header class="head">
      <h2>Как это работает</h2>
      <p>
        Никакой магии: заявка → модерация → ссылка/промокод → выплаты. Модерация
        нужна для качества и честного учёта.
      </p>
    </header>

    <div class="grid">
      <article class="card">
        <div class="top">
          <span class="num">01</span>
          <span class="pill">30 секунд</span>
        </div>
        <h3>Оставляешь заявку</h3>
        <p>Заполняешь форму (вверху или ниже). Указываешь TG-ник и площадку.</p>
      </article>

      <article class="card">
        <div class="top">
          <span class="num">02</span>
          <span class="pill">24–48ч</span>
        </div>
        <h3>Мы проверяем площадку</h3>
        <p>
          Чтобы защитить программу от спама и сохранить прозрачную атрибуцию.
        </p>
      </article>

      <article class="card">
        <div class="top">
          <span class="num">03</span>
          <span class="pill">доступ</span>
        </div>
        <h3>Выдаём ссылку/промокод</h3>
        <p>Плюс материалы: посты, баннеры, сценарии, примеры связок.</p>
      </article>

      <article class="card">
        <div class="top">
          <span class="num">04</span>
          <span class="pill pill--accent">выплаты</span>
        </div>
        <h3>Рекомендуешь → получаешь</h3>
        <p>Мы считаем оплаты и выплачиваем вознаграждение 1–2 раза в месяц.</p>
      </article>
    </div>
  </div>
</section>

<style>
  .p3.steps {
    margin: 20px;
    border-radius: 28px;
    background: #070a12;
    color: #eef2ff;
    padding: clamp(26px, 5vw, 56px) 0;
    position: relative;
    overflow: hidden;
  }
  .p3.steps::before {
    content: "";
    position: absolute;
    inset: -2px;
    background:
      radial-gradient(
        520px 260px at 15% 25%,
        rgba(96, 165, 250, 0.16),
        transparent 60%
      ),
      radial-gradient(
        520px 260px at 80% 60%,
        rgba(249, 115, 22, 0.14),
        transparent 60%
      );
    pointer-events: none;
  }
  .wrap {
    position: relative;
    z-index: 2;
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 clamp(16px, 3vw, 28px);
  }
  .head {
    max-width: 72ch;
    margin-bottom: 18px;
  }
  h2 {
    margin: 0 0 10px;
    font-size: clamp(24px, 3.6vw, 40px);
    letter-spacing: -0.02em;
    line-height: 1.1;
    font-weight: 950;
  }
  .head p {
    margin: 0;
    color: rgba(238, 242, 255, 0.8);
    line-height: 1.65;
    font-weight: 650;
    font-size: 16px;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
    margin-top: 18px;
  }

  .card {
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    padding: 14px;
    backdrop-filter: blur(12px);
    position: relative;
    overflow: hidden;
  }
  .card::after {
    content: "";
    position: absolute;
    inset: auto -90px -100px auto;
    width: 240px;
    height: 240px;
    background: radial-gradient(
      circle at 30% 30%,
      rgba(167, 139, 250, 0.18),
      transparent 60%
    );
    transform: rotate(10deg);
    pointer-events: none;
  }
  .card > * {
    position: relative;
    z-index: 1;
  }

  .top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 10px;
  }
  .num {
    font-weight: 950;
    letter-spacing: 0.08em;
    font-size: 12px;
    opacity: 0.9;
  }
  .pill {
    padding: 7px 10px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    font-size: 12px;
    font-weight: 850;
    color: rgba(238, 242, 255, 0.88);
  }
  .pill--accent {
    background: linear-gradient(
      135deg,
      rgba(34, 197, 94, 0.22),
      rgba(96, 165, 250, 0.22)
    );
    border-color: rgba(255, 255, 255, 0.18);
  }

  h3 {
    margin: 0 0 8px;
    font-weight: 950;
    font-size: 18px;
    letter-spacing: -0.01em;
  }
  .card p {
    margin: 0;
    color: rgba(238, 242, 255, 0.78);
    line-height: 1.55;
    font-weight: 650;
  }

  @media (max-width: 980px) {
    .grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 560px) {
    .p3.steps {
      margin: 12px;
      border-radius: 22px;
    }
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
