# 🚀 Git Helper - Полное руководство для начинающих

## 📋 Содержание

1. [Основы Git](#основы-git)
2. [Настройка Git](#настройка-git)
3. [Основные команды](#основные-команды)
4. [Рабочий процесс](#рабочий-процесс)
5. [Решение проблем](#решение-проблем)
6. [Полезные команды](#полезные-команды)
7. [Примеры ситуаций](#примеры-ситуаций)

---

## 🔧 Основы Git

### Что такое Git?

Git - это система контроля версий, которая:

- 📝 Отслеживает изменения в файлах
- 🔄 Позволяет откатываться к предыдущим версиям
- 👥 Позволяет работать в команде
- ☁️ Синхронизирует код с GitHub

### Основные понятия:

- **Репозиторий** - папка с проектом, которую отслеживает Git
- **Коммит** - снимок состояния проекта в определенный момент
- **Ветка** - отдельная линия разработки
- **GitHub** - облачное хранилище для Git репозиториев

---

## ⚙️ Настройка Git

### Первоначальная настройка (делается один раз):

```bash
# Установить имя пользователя
git config --global user.name "Ваше Имя"

# Установить email
git config --global user.email "ваш-email@example.com"

# Проверить настройки
git config --list
```

### Настройка репозитория:

```bash
# Инициализировать Git в папке проекта
git init

# Подключить к GitHub репозиторию
git remote add origin https://github.com/USERNAME/REPOSITORY.git
```

---

## 🎯 Основные команды

### 1. Проверка статуса

```bash
# Посмотреть статус файлов
git status

# Посмотреть историю коммитов
git log

# Посмотреть изменения в файлах
git diff
```

### 2. Добавление файлов

```bash
# Добавить все файлы
git add .

# Добавить конкретный файл
git add filename.astro

# Добавить все файлы в папке
git add src/

# Добавить все файлы определенного типа
git add *.astro
```

### 3. Создание коммитов

```bash
# Создать коммит с сообщением
git commit -m "Описание изменений"

# Создать коммит с подробным описанием
git commit -m "Краткое описание" -m "Подробное описание изменений"

# Добавить файлы и создать коммит одной командой
git commit -am "Описание изменений"
```

### 4. Работа с GitHub

```bash
# Отправить изменения на GitHub
git push

# Отправить изменения в первый раз
git push -u origin main

# Получить изменения с GitHub
git pull

# Скачать изменения без слияния
git fetch
```

---

## 🔄 Рабочий процесс

### Ежедневный workflow:

```bash
# 1. Проверить статус
git status

# 2. Добавить изменения
git add .

# 3. Создать коммит
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push
```

### Работа с ветками:

```bash
# Посмотреть все ветки
git branch

# Создать новую ветку
git branch feature-name

# Переключиться на ветку
git checkout feature-name

# Создать и переключиться на ветку
git checkout -b feature-name

# Слить ветку в main
git checkout main
git merge feature-name

# Удалить ветку
git branch -d feature-name
```

---

## 🚨 Решение проблем

### Отмена изменений:

```bash
# Отменить изменения в файле (до добавления в staging)
git checkout -- filename.astro

# Убрать файл из staging (после git add)
git reset HEAD filename.astro

# Отменить последний коммит (сохранив изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалив изменения)
git reset --hard HEAD~1
```

### Конфликты при слиянии:

```bash
# Если возник конфликт при git pull
# 1. Откройте файл с конфликтом
# 2. Найдите маркеры конфликта:
#    <<<<<<< HEAD
#    ваш код
#    =======
#    код с GitHub
#    >>>>>>> branch-name
# 3. Удалите маркеры и оставьте нужный код
# 4. Добавьте файл: git add filename.astro
# 5. Завершите слияние: git commit
```

### Проблемы с push:

```bash
# Если push не работает из-за изменений на GitHub
git pull
# Решите конфликты если есть
git push

# Принудительный push (ОСТОРОЖНО!)
git push --force
```

---

## 🛠️ Полезные команды

### Просмотр истории:

```bash
# Краткая история
git log --oneline

# История с графиком веток
git log --graph --oneline --all

# История конкретного файла
git log -- filename.astro

# Показать изменения в последнем коммите
git show
```

### Работа с файлами:

```bash
# Удалить файл из Git (но оставить локально)
git rm --cached filename.astro

# Переименовать файл
git mv old-name.astro new-name.astro

# Игнорировать файлы (.gitignore)
echo "filename.astro" >> .gitignore
```

### Информация:

```bash
# Показать информацию о репозитории
git remote -v

# Показать разницу между ветками
git diff main..feature-branch

# Показать статистику изменений
git diff --stat
```

---

## 📝 Примеры ситуаций

### Ситуация 1: "Я случайно удалил файл"

```bash
# Восстановить файл из последнего коммита
git checkout HEAD -- filename.astro
```

### Ситуация 2: "Я сделал коммит с неправильным сообщением"

```bash
# Изменить сообщение последнего коммита
git commit --amend -m "Правильное сообщение"
```

### Ситуация 3: "Я хочу откатиться на 3 коммита назад"

```bash
# Мягкий откат (сохранить изменения)
git reset --soft HEAD~3

# Жесткий откат (удалить изменения)
git reset --hard HEAD~3
```

### Ситуация 4: "Я хочу посмотреть, что изменилось в файле"

```bash
# Показать изменения в файле
git diff filename.astro

# Показать изменения в последнем коммите
git show HEAD -- filename.astro
```

### Ситуация 5: "Я забыл добавить файл в последний коммит"

```bash
# Добавить файл
git add forgotten-file.astro

# Добавить к последнему коммиту
git commit --amend --no-edit
```

---

## 🎯 Лучшие практики

### Сообщения коммитов:

✅ **Хорошо:**

- `"Add new article about chemical reactions"`
- `"Fix responsive design for mobile devices"`
- `"Update course content and images"`
- `"Remove unused like/view counters"`

❌ **Плохо:**

- `"fix"`
- `"update"`
- `"changes"`
- `"asdf"`

### Частота коммитов:

- Делайте коммиты часто (каждые 1-2 часа работы)
- Один коммит = одна логическая задача
- Не накапливайте много изменений в одном коммите

### Работа с ветками:

- `main` - основная ветка для продакшн кода
- `feature/название` - для новых функций
- `fix/название` - для исправлений багов

---

## 🚀 Полезные алиасы (опционально)

Добавьте в `.gitconfig` для упрощения команд:

```bash
git config --global alias.st status
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.unstage 'reset HEAD --'
git config --global alias.last 'log -1 HEAD'
git config --global alias.visual '!gitk'
```

Теперь можно использовать:

- `git st` вместо `git status`
- `git co` вместо `git checkout`
- `git ci` вместо `git commit`

---

## 📚 Дополнительные ресурсы

- [Официальная документация Git](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Интерактивный Git tutorial](https://learngitbranching.js.org/)

---

## 🏷️ Версионирование и релизы

### Создание версий (тегов):

```bash
# Создать версию
git tag v1.0 -m "Version 1.0 - Initial release"

# Отправить тег на GitHub
git push origin v1.0

# Посмотреть все версии
git tag

# Переключиться на конкретную версию
git checkout v1.0
```

### Pull Request (PR) - запрос на слияние:

```bash
# 1. Создать feature ветку
git checkout -b feature/new-feature

# 2. Сделать изменения и закоммитить
git add .
git commit -m "Add new feature"
git push -u origin feature/new-feature

# 3. На GitHub создать Pull Request
# 4. После одобрения - слить в main
```

### Работа с ветками для версий:

```bash
# main - стабильная версия (продакшн)
# develop - версия в разработке

# Создать ветку для новой функции
git checkout -b feature/new-function

# Слить функцию в develop
git checkout develop
git merge feature/new-function

# Создать релиз
git checkout main
git merge develop
git tag v1.1 -m "Version 1.1 - New features"
git push origin main v1.1
```

---

## 🆘 Экстренные команды

Если что-то пошло не так:

```bash
# Отменить все локальные изменения
git reset --hard HEAD

# Очистить неотслеживаемые файлы
git clean -fd

# Сбросить до состояния на GitHub
git fetch origin
git reset --hard origin/main

# Вернуться к конкретной версии
git checkout v1.0
```

**Помните:** Git сохраняет историю, поэтому почти всегда можно восстановить данные!

---

_Создано для проекта school-media_ 🧪
